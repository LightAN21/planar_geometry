function point_mouse_move(event) {
    var pos = mouse_pos(canvas_div, event);
}

function point_mouse_down(event) {
    var pos = mouse_pos(canvas_div, event);
}

function point_mouse_down_move(event) {
    var pos = mouse_pos(canvas_div, event);
}

function point_mouse_up(event) {
    var pos = mouse_pos(canvas_div, event);
}
